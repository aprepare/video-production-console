# 二创提示词与流程 v3.10 交付

2026-09-05。依据四账号 Astra 稿件审计，在用户确认后修改提示词与执行代码。服务已于 10:41 重启，当前监听 2030 的进程为 54216。

## 已实施

| 层级 | 本次修改 |
|---|---|
| 写手 | 保留可信主题、节奏与叙事张力；事实问题局部纠正，核心前提失实则重建，不把整篇变成逐句核查报告。 |
| 前置事实 | 区分原文对应时期与当前核查日期；搜索计划、空结果不算核查完成；来源至少有日期与链接，或可验算的正确算式；保留原始响应供追溯。无可用核查结论时保留初稿并标记失败，停止后续机械校验与终审。 |
| 情报体积 | 素材包最多保留 6 条代表性禁用意象、1 个中心选项、1 个场景、3 个短语替换；改为可选参考，避免长禁令清单左右正文。已有工作流未启用爆款模式库节点，本次没有重新接入。 |
| 数字校验 | 原文修辞数字不再全部强制补回；核准事实按要求保留或省略；修复年份区间、日期识别与等值单位匹配，保留数值精度；定向返工仍未修好核准数字则失败，不再警告放行。 |
| 审稿 | 聚焦事实、论证、相似度、课程匹配和表达问题；要求指出证据与修改位置，避免纯数字格式返工；审稿调用失败不会成为完成态。 |
| 课尾 | 统一到真实的 9 节家庭财务基础课程。建议 220–350 字，价格提及 1–2 次，课程名 1–2 次、橱窗动作 1 次；用课程的具体用途承接，不用反复压价和否定式劝退填充。 |
| 交付与状态 | 保留 CTA，简介与话题分开；已有失败结果优先于落盘稿件判断，避免失败显示完成；重试前清理上一轮结果信封，避免旧失败污染新结果。 |

## 生效范围与备份

- Go 内统一提示词版本：`骨肉分离v3.10 核查分层与精简课尾 2026-09-05`。
- 已同步 11 份运行配置：当前写手、内置提示词库、情报/审稿覆盖配置、全局工作流、7 个账号工作流。
- 同步脚本检查了除提示词和注入规则以外的工作流配置保持一致；模型、推理强度、通道、Fast、工作流连线和生产选项均未调整。
- 提示词备份：`video-console-data/remix_lab/backup-policy-20260905-103520`。
- 程序备份：`dist/video-production-console.before-v3.10-20260905-104104.exe`。
- 配置变更哈希记录：[同步记录](2026-09-05-policy-v3.10-sync.json)。安装文件与进程记录：[部署记录](2026-09-05-v3.10-deployment.json)。
- 历史稿件、运行快照和数据库历史状态保留原样。

## 验证

- 本次完整相关测试：`go test ./internal/agentruntime/openaicompat ./internal/remixlab ./internal/httpapi ./internal/store -count=1` 全部通过。
- 最后的数字硬错误拦截修改后，重新运行 `go test ./internal/agentruntime/openaicompat ./internal/remixlab -count=1`，全部通过。
- `go vet ./internal/agentruntime/openaicompat ./internal/remixlab ./internal/httpapi ./cmd/remix-policy` 通过。
- `go build -trimpath -o dist/video-production-console.policy-v3.10.exe ./cmd/console` 成功；安装文件与该产物 SHA-256 相同。
- `git diff --check` 通过，有仓库现存换行转换提示。
- 配置同步再次 dry-run 返回空变更，验证幂等。
- 重启前检查二创及生产任务均无正在运行或排队的条目；重启后首页、JS 与 CSS 均 HTTP 200，2030 端口由新进程监听。
- 内嵌资源非严格检查通过；现存未跟踪的前端资源使严格发布检查仍有缺口，本次未声称完成正式发布包验收。

## 下一轮 Claude 对照

选择当前 API 通道提供的 Claude 型号即可沿用本流程，具体模型名透传。写手与审稿是独立配置；若要先看 Claude 写作表现，可只更换写手，保留同一套情报和审稿条件。

本次没有发起 Claude 或其他付费模型试跑。已验证代码和本地服务，实际通道对推理参数与 Fast 的支持、生成文案质量仍需下一轮运行确认。来源格式与算式校验提高了核查门槛；链接是否真实支持结论仍由核查与审稿承担，格式合格本身不代表事实正确。
