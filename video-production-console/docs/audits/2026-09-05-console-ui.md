# 控制台界面重构验收

日期：2026-09-05。风格已由用户确认：深蓝灰导航、明亮工作区、青蓝强调与轻量动效。

## 完成范围

- 文案与混剪、图文制作、AI短片采用统一全局导航；设置、主题、退出固定收纳。
- 文案工作区突出四阶段当前状态；历史独立滚动，画布聚焦输入并随容器尺寸调整。
- 文案/审稿/发布字段、项目工作台、图文创建、分镜、交付区和设置/素材/任务/账号弹窗统一布局与视觉。
- 定稿先保存再生产；保存失败保留输入；任务提交互斥并在弹窗反馈错误。
- 原文按账号暂存；图文/短片创建和短片编辑按项目暂存，跨页面切换可恢复。
- 修复导入重试重复建项目、独立历史项目打不开、文案库刷新覆盖编辑，以及短片旧轮询响应跨项目覆盖的问题。
- 生成草稿、待人工确认、已发布使用不同状态；支持窄屏、暗色、键盘分类导航、减弱动态效果。

## 验证

- 最终前端 Vitest：24 文件，261 项通过，4 项既有跳过。
- TypeScript 检查、lint、常规 git diff --check 通过。
- build:verify、build:embed、嵌入资源引用存在性校验通过。新增构建资源未暂存，Strict 的 Git 跟踪检查未启用。
- Go：internal/webui 与 cmd/console 测试通过，运行程序构建成功。
- 浏览器：三个制作入口、独立历史项目、设置弹窗；桌面/平板/窄屏/暗色检查；减弱动效模拟后已恢复；浏览器无 error 日志。
- 原地址 http://127.0.0.1:2030/ 返回新版 JS/CSS，静态资源 HTTP 200；同期另一个任务也更新了程序，保留其最新构建，最终服务 JS 与当前嵌入目录哈希一致，并包含新导航与任务提交保护。
- 更新前确认无运行任务；保留 dist/video-production-console.before-ui-20260905-105711.exe。程序已在后台运行；临时 Vite 预览服务已关闭。

## 范围说明

未触发真实内容生成、配音、混剪或发布；相关提交行为使用假 API 回归验证。保留其他 AI 已有的提示词、Fast 模式及后端改动，未提交 Git。构建仍提示单个 JS 包超过500kB，不影响本次运行。
