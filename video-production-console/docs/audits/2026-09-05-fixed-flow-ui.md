# 固定流程界面验收

用户要求：固定工作流以步骤按钮/方框展示，不再使用画布；保留本地同期代码。

## 变更

- 编辑页和运行页共用 FixedFlowSteps，分为文案创作与混剪制作，点选步骤查看已有配置或实际结果。
- 移除运行时 ReactFlow、拖拽、缩放、连线、自动排版。保留旧配置的节点 ID、顺序、依赖、坐标和 production 配置，不迁移或重写工作流数据。
- 编辑页默认展开原文，保留按账号暂存、模型配置、Fast、导入定稿、撤销配置和重跑功能。
- 运行页保留实时轮询、全部历史 agent、失败步骤重试、稿件编辑、生产确认与发布处理；增加读取失败后的重试入口。
- 桌面步骤栏与详情独立滚动；窄屏上下排列，全屏容器可滚动；键盘焦点与减弱动效适配。

## 验证

- Vitest：27 个文件通过，273 项通过，4 项既有跳过。使用 maxWorkers=2 避免并发测试争抢资源导致 5 秒超时。
- TypeScript、lint、git diff --check 通过；build:embed 成功。
- go test ./internal/webui ./cmd/console 通过；当前源代码构建程序成功。
- 浏览器验证编辑步骤切换、历史稿件真实阶段与输出、待确认/已跳过/待执行状态，无 ReactFlow DOM，桌面无水平溢出。
- 窄屏全屏检查：12 个固定按钮，容器 overflow-y:auto，长内容可滚动，无水平溢出。
- 未触发真实内容生成、配音、混剪或发布；有副作用的流程使用假 API 回归测试。

## 本机更新

更新前检查无正在运行的制作任务，并核对程序哈希及构建后源文件没有变化。旧程序备份为 dist/video-production-console.before-fixed-flow-20260905-145547.exe；新版本在原地址 http://127.0.0.1:2030/ 后台运行。

保留用户已有及同期修改，没有执行 Git reset、工作区清理、回退源文件或 Git 提交。遗留的 InitialFlowViewport 源文件与依赖清单未删除，当前编辑页和运行页不再引用该组件。
