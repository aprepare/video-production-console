# 2026-09-08 GitHub 主分支同步审查

## 范围与合入方式

- 基线：`master` / `97f80ea8a3428c451ca216323c08c05df1625dfa`。
- 开发分支：`codex/video-production-console` / `2f748e00009c37f08181afd878732baed5ea29c1`。
- 基线到开发分支有 65 个提交、681 个变更文件。基线是开发分支的祖先，可快进合入，保留原提交历史。
- `video-production-console/二创工作区` 的 136 个已提交文件全部保留。本次不改成稿、参考素材和经验库正文。
- 原工作目录的 9 个未提交修改及 2 个未跟踪文件不属于本次合入；在独立干净副本中审查和验证。

## 发现并修复

1. **参考模型配置被清空。** `RoleModelsDialog` 保存时固定传入空参考列表，`RerunDialog` 又覆盖接口返回的列表。恢复现有参考模型控件，保存已有选择，并保留重跑接口中“未传 references”与“显式清空列表”的区别。已有三个回归测试在修复前稳定失败，修复后通过。
2. **工作区文件可经符号链接越界。** 原先只有字符串路径校验，实际文件读取、写入仍跟随外部链接。改用 Go `os.Root` 限制实际文件访问；写入前固定父目录句柄，比较、临时文件和替换都在该目录内完成。Windows 实际创建的外部符号链接用例已验证，未跳过。
3. **文档保存存在丢稿窗口。** 改为临时文件写完并 Sync 后替换，避免直接截断原稿；GET 返回正文 SHA256 revision，PUT 要求 expected_revision，已发生的外部修改返回 409。前端保留冲突草稿、保存期间的新输入，防止重复保存及旧读取/保存响应覆盖新文件；窗口 focus 不再打断正在进行的文件切换。
4. **测试辅助脚本依赖模块自动加载。** fake-codex 的 Get-FileHash 在 Go 启动的 PowerShell 子进程中不可用，导致测试返回空哈希。改用 .NET SHA256，保留正式资产哈希校验。
5. **浏览器测试未适配当前界面。** 补齐文案创作台接口模拟，使用当前导航链接、主题入口、范围明确的阶段和描述选择器，以及现行深色主题颜色。6 项端到端测试全部通过。
6. **GitHub 自动验证未生效。** 工作流原位于项目子目录，移到仓库根 `.github/workflows`，明确 Go/Node/命令的项目路径；PowerShell 验证脚本增加每个外部命令的退出码检查，测试并发限制为 2。已用失败退出码 23 验证脚本会立即停止。

## 验证记录

- `go test ./...`：全部通过；最后的父目录句柄调整再次通过工作区与 HTTP 定向测试。
- `go vet ./...`：通过。
- 前端全量 Vitest：34 个测试文件通过；保留原有 4 个被跳过的智能体聊天测试，未通过跳过用例处理本次失败。
- 工作区前端新增/现有 8 项用例通过，包括保存期间继续输入、409、读取乱序、切换文件后保存响应、focus 与路由加载冲突。
- `npm run test:e2e -- --workers=2`：6 项通过。
- 前端 lint 无错误，保留现有非阻断警告；TypeScript 与生产构建通过。嵌入 Go 的前端资源随修复重新生成。

## 验证边界

这是合入审查，重点检查分支历史、接口/存储边界及自动化回归，并非对 681 个文件逐行做完整审计。没有使用真实模型服务执行付费生成，也没有操作生产数据库或启动剪映导出。

工作区的 revision 是乐观冲突检测；外部程序不参与应用写锁，最后一次检查与文件替换之间仍有极窄并发窗口。临时文件 Sync 与替换不等于断电持久性保证，也不承诺保留 ACL、扩展属性等全部文件元数据。
