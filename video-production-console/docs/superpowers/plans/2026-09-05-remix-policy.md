# 二创规则统一与真实课程承接实施计划

> 执行依据：用户已确认分层审查修改清单，并提供九节课商品详情；在当前已有改动的工作树内完成，保留其他任务的模型选择及界面修改。

**Goal:** 保留二创的原文机制，确保事实结论、发布字段和审稿修订可靠传递，课程承诺与九节真实课纲一致。

**Architecture:** 在 openaicompat 集中维护版本化公共规则、课纲和角色提示，内置目录与本地采用副本由同一来源生成。保存每次运行的审稿上下文（规则、事实、阈值），首轮及批注重做共同使用。默认工作流移除 patterns，库文件保留。

**Tech Stack:** Go、JSON、现有 React 控制台；fake ChatClient 离线测试。

- [x] 复现回归：包装 JSON 局部修改丢字段、长事实结果头截、非法审稿 verdict、审稿提高相似度、发布数量越界。运行 `go test ./internal/agentruntime/openaicompat -run TestEditorial -count=1`，记录失败。
- [x] 修正文替换：parseRemixDraft 成功后保持完整字段，修订 CTA 缺省继承；只对 continuous_script 更新，不返回裸正文。
- [x] 事实提取：从带搜索过程的回复中选择最后一个完整事实 JSON；保留全部事实结论，不截碎 JSON；非法事实输出显式标记不可用。保存结构化事实供写手、自检、审稿复用。
- [x] 统一课程和角色：新建版本化公共规则和课程资料；九节课按截图录入，价格按用户既有资料为5元；课尾接家庭收支/储蓄/负债/风险管理，排除数据变现等课外承诺。缩短写手/user/审稿与情报提示。
- [x] 统一核验：事实表允许有来源的修正和新增数字；保留原文相似度基准，审稿不能使相似度、数字或格式变坏；检测越界结论，保留审前稿并记录待复核原因。价格、课程次数、长度保持本轮已确认标准。
- [x] 运行接线：首轮保存上下文；返工读取同一上下文；审后复检正文和发布字段；事实不完整和字段错误在交付警告中展示。
- [x] 更新本地 active/library/workflow（先备份），同步内置 bone_flesh 与说明。仅移除当前 patterns 节点及关联边，保留素材库和历史快照。
- [x] 验证：先聚焦新回归，再 `go test ./internal/agentruntime/openaicompat ./internal/remixlab ./internal/httpapi -count=1`、相关包 vet 和构建。使用旧样本的存档做离线复核，不覆盖历史稿件。记录构建与实际模型验证的边界。

验收：100元纠错结论不被截掉；带 The 前缀的完整发布字段被保留；17.20%→25.95%的审稿修订被挡回；课程承接不声称课纲以外的操作教学；默认工作流不再调用 patterns。仅凭两次历史运行不推断关闭模式库能提升转化率。
