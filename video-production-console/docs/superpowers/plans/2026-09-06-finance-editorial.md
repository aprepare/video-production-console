# 财经编辑混合画风

用户已确认：新建项目默认纸张拼贴为主、概念关系用微缩模型；旧项目不迁移；分镜可手动切换。

1. 后端新增 finance_editorial 策略、paper_collage / miniature 素材画风。保留原预设与旧数据默认。新增项目空画风使用新策略。
2. 分镜策划请求同时指定策略和每镜 style_key；先按文意选表现再构思场景。模型回复解析、机械回退借用、最终编号、保存和读取均保留合法选择；无选择保守回退纸张拼贴。
3. 混合项目分镜 PATCH 仅允许两种素材风格；改风格只使该镜旧图失效。普通单风格项目沿用原规则。新提示词共用米白、深绿、朱红，人物按剪纸或模型处理。
4. 前端改项目默认与说明；混合项目分镜编辑增加风格选择并提交。子代理只负责前端三个指定文件，主代理验收。
5. 回归测试覆盖策划传参、单镜保存与重新读取、非法风格拒绝、旧项目不变、新建默认、前端提交。运行 aishorts 和相关 httpapi / vitest、类型检查及构建，检查差异后重启并验证健康。

不调用付费模型，不改写已有项目素材，不改配音或剪映组装流程。
