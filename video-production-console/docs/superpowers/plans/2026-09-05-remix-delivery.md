# 二创提示词与流程修订交付

版本：骨肉分离 v3.9.1，2026-09-05。

## 已完成

| 层次 | 实际问题 | 本次处理 |
| --- | --- | --- |
| 写手 | 激活提示词约7101字，与内置版分叉，示例与重复指令争夺注意力 | 统一到1374字的规则与真实课纲；用户模板只保留任务、批注、原文；内置、激活、提示词库同源 |
| 前置情报 | 从搜索过程头截3600字，事实纠错落在截断范围外；风格优先级压过事实 | 提取含 source_facts 的完整对象，排除搜索过程及无关尾对象；事实与课程资料优先；新增资料允许0～2条，意象建议去掉三套课尾 |
| 机械处理 | 修正文时包装JSON丢失发布字段；锁数字可能要求恢复原文错误值 | 保留解析后的完整字段；核准纠正值进入数字检查，有来源的新增年份允许出现但不强制使用 |
| 审稿 | 工作流事实文件名与审稿读取名不一致；审后可以把重合和数字改坏 | 工作流同步事实文件；保存规则、事实、阈值和审稿提示快照，返工沿用；修订复检重合、篇幅、年份、数字和发布字段，新问题退回审前稿 |
| 发布字段 | 数量上限、标题下限、描述40字及标签规则存在缺口 | 补齐确定性格式检查；正文外话题及未获支持的金额/利率进入终稿警告；字段修复保持正文 |
| 批注保存 | 后台运行状态写入可能用旧记录覆盖新批注 | 状态更新不再写 comment，批注通过专属更新接口保存；存储回归和HTTP往返连续10次通过 |
| 模式库 | 全局工作流已移除，7个账号工作流仍保留 patterns | 全局及7个账号工作流同步，移除节点及关联边；库文件保留作手动试验素材 |

课尾改为从本条话题自然落到观众的家庭财务问题，只选真实九节课中最相关的1～2项承接，不背目录、不借宏观话题承诺教授数据变现或智能合约操作。正文仍保持原主题。课尾380～480字、课名1～2次、橱窗1～3次、五块钱3～5次沿用本轮约定。

九节课已按商品截图录入：月度收支计划、存钱与储蓄系统、财务目标、收入支出负债梳理、减支、消费管理、财务安全网、个人财务调整方向、工具包。价格沿用用户既有资料的5元；图片本身没有显示价格。

## 验证证据

- 13个 editorial 回归用例通过，包括包装JSON、长事实结论、工作流事实到审稿、规则快照、核准纠错与可选新数据、非法审稿结论、新增年份/利率、发布格式、实际旧稿。
- 历史数据资产稿实际复核：审前重合17.20%，审后25.95%，新版拒绝此修订。
- 历史事实输出85110字节，提取为15533字节完整结构化结论，保留100元纠错信息。历史原文件保持原样。
- `go test ./internal/agentruntime/openaicompat ./internal/remixlab ./internal/httpapi ./internal/store -count=1`。
- `go vet ./internal/agentruntime/openaicompat ./internal/remixlab ./internal/httpapi ./cmd/remix-policy`。
- `go build -o dist/video-production-console.policy-3.9.1.exe ./cmd/console`。

## 文件与回滚

- 规则来源：`internal/agentruntime/openaicompat/editorial_policy.go`。
- 事实和审后校验：`internal/agentruntime/openaicompat/editorial_context.go`。
- 编译规则导出：`go run ./cmd/remix-policy`；本次导出存于同目录 `2026-09-05-remix-policy-export.json`。
- 10份配置备份：`video-console-data/remix_lab/backup-policy-20260905-092656/`。
- 同步路径、前后SHA256和被移除节点：同目录 `2026-09-05-remix-config-sync.json`。
- 所有账号的模型、推理强度、通道、机械阈值及生产配置均逐项核验保持原值。
- 编译产物同步至 `dist/video-production-console.exe`，更新前核查该服务没有运行。原可执行文件备份为 `dist/video-production-console.before-policy-20260905-092919.exe`。下次从正常入口启动即加载新版。

## 验证边界

这轮是源码、配置及历史样本的离线回归；没有额外调用外部模型出新稿，也没有发布视频或做转化率对比。关闭模式库的依据是减少与已有完整原文竞争的结构建议，尚无转化提升的实测结论。

机械校验覆盖明确的年份、百分比、金额及结构化纠错；未带单位的次数、中文数字、算术的语义关系和来源真实性仍需事实核查员与审稿判断。课程过渡句的边界、380～480字课尾及营销次数由共同提示词要求审稿核对，未用关键词猜边界做硬切。历史任务若没有 editorial_context.json，首次返工才建立可用规则快照，不能还原其当时未保存的情报。

其他AI已有的模型设置、运行状态与界面改动保留；本报告的验证结论限于上列命令和本轮二创链路。
