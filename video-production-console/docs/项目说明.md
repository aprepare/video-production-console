# 视频生产控制台 · 项目说明

> 本仓库**只保留这一份说明**。界面设计 token 见根目录 `DESIGN.md`；可交互架构图见 `docs/archify/runtime.architecture.html`。历史设计稿、路线图、优化工单已删除，考古走 git。
>
> 日期：2026-08-22。以本 worktree 代码为准。给新协作者：先读第 1、2、3 节了解产品，再看第 11 节「近期更新」快速对齐我们最近做了什么。

---

## 1. 这是什么

本地单机视频生产控制台。Go 管鉴权、设置、SQLite、任务调度、图文批次、素材库；React 提供风景混剪看板和图文制作。不打开微信视频号，不托管成片上传。成片在剪映里导出。

默认地址 `http://127.0.0.1:2030`（本机常见也监听 `0.0.0.0:2030`）。权威库 `video-console-data/console.db`。根目录遗留的 `video-console.db` 不是权威库。

跑的是 `dist\video-production-console.exe` 时：改 Go / 前端必须停进程 → 必要时 `npm --prefix web run build:embed` → 重编 exe → **从仓库根目录**再启动。Vite `npm run dev` 的页面不是 exe 里那份。

首次安装（库里还没有管理员）必须先设：

```powershell
$env:VIDEO_CONSOLE_INITIAL_PASSWORD = "你的初始口令"
.\dist\video-production-console.exe
```

已有管理员后不再读这个变量。首次登录后立刻改密。不要把控制台暴露到公网或公共 Wi-Fi。

### 1.1 两条工作线，不要混

| 线 | Git 分支 | 目录 | 给谁 |
|---|---|---|---|
| 本机开发 / 风景混剪 | `codex/video-production-console` | **本 worktree** | 自己验收。登录后默认风景混剪 |
| 伙伴 EXE | `feat/partner-exe-rollout` | 另一棵 worktree（`partner-exe-rollout`） | 合作伙伴。最新包 **0.2.0**（已合入主线全部功能） |

本 worktree 不打包、不激活伙伴版。伙伴网关/安装说明只在那条分支。伙伴版无自动更新：合并主线 → `scripts/build-partner.ps1` → 发新安装包（启动器装新版自动保留旧版可回滚）。伙伴所有模型流量走钉死的网关（`https://23.138.12.112:2443`），设置页永不暴露任何 Base URL / API Key。

---

## 2. 当前开放的入口

登录后 **`/` 直接进入风景混剪**（URL 写成 `/projects?mode=scenic`）。不再出现四张卡片的「选择制作方式」页。

看板右上角「图文制作」切到 ZIP 线；图文制作页右上角「风景混剪」切回来。

| 入口 | 地址 | 产出 | 状态 |
|---|---|---|---|
| 风景混剪 | `/` 或 `/projects?mode=scenic` | 口播配风景镜头，剪映草稿 | **开放**。不按口播对画面 |
| 图文制作 | `/image-projects` | 最多 18 张竖版图 + ZIP | **开放**。**不**出剪映草稿 |
| 电影混剪 | `/projects?mode=movie` | 口播配电影/B-roll 切镜 | **已屏蔽，后续再开。** 旧地址改写到风景混剪 |
| 图文视频 | `/image-videos` | 口播拆段生图 → 无字幕剪映草稿 | **已屏蔽，后续再开。** 旧地址改写到风景混剪 |

后端 API、技能和 `ImageVideoStudio` 代码还在，只是 `App.tsx` 不挂入口。不要用旧书签测电影混剪或图文视频。`/projects?mode=image-video` 同样改写到风景混剪。

---

## 3. 怎么用

### 3.1 风景混剪（日产主路径）

1. 打开控制台即风景混剪看板。打开或新建项目（项目属于账号）。右上角可切到图文制作。
2. 粘贴同行原文 →「保存原文并开始二创」。写入 `source_script`，创建 `remix.standard`。
3. 二创**硬切** OpenAI 兼容接口（设置「二创服务地址 / 二创模型 / 二创 API 密钥」），不走 Codex CLI。模型名原样发送，不剥 `cursor-`。
4. rewrite 默认版本戳：`文案进化台 2026-08-21 爆款回流定稿`（`internal/agentruntime/openaicompat/run.go` `RewritePromptStamp` / `buildWriterPrompt`）。规则：先锁六件套机器；关键数字必须原词；对仗钩子不能写软；开场禁止锚点/换锚/货币/认知等中老年听不懂的黑话；原文有真实日期时开头优先写成截止日紧急提醒（禁止编造日期）；互动引导只许软写——转发走家庭责任、评论留许愿口，「快转发/扣1/接接接」一律判失败；禁止按「第 N 个难题」对照译。wash 路径未改。落盘以 `continuous_script` 为准。
5. 连续文案就绪后**停住**，等操作员确认文案再点「生成口播稿」：`remix.spoken_lines`，写入 `spoken_script`。不要自动续跑口播。规则：一句一行、每行不超过 9 个字（只计汉字和字母，阿拉伯数字和标点不计）、中间无空行、行尾空格；中文年份和「百分之」改阿拉伯数字；大数字优先用阿拉伯数字；《财富觉醒方法论》不拆开。后续配音字幕按这些行切，不要恢复 `remix.spoken_format`。口播稿就绪后仍自动标字幕关键词（失败回落本地词表）。项目资产里有「字幕关键词」卡：点「查看」按口播行展示红/金标注，可增删后保存；「重标关键词」只出现在这张卡上。保存走 `POST /api/projects/{id}/assets/caption_keywords`，会让已有混剪草稿失效。
6. 「生成配音与字幕」→ `POST /api/projects/{id}/narration`（火山 TTS + 词级 SRT）。配音读口播稿，不重写 `spoken_script`。手动上传走 `POST /api/projects/{id}/assets/narration`，不自动出字幕。两条路由不能抢占。
7. 「开始风景混剪」→ 本机 `montage-script-run` → Go 写 `production_plan.json` → Python skill 造明文草稿 → `montage.Coordinator` 登记剪映。
8. skill snapshot 含计划版本 `2.0` 时走 `BuildV2`。口播字幕轨 **打开**：行级 SRT 一对一上屏（`spokenCaptionsFromLineCues`），不再把多行粘合后按字切开。判定按口播稿内容字预算（汉字+字母，数字不计），带 `1.45%` / `2023年` 的行仍走一对一。风景任务 notes 为 `scenic_mixed_pool`：打散 `media_index` 风景/城市/财经并混 catalog B-roll，**不按口播对画面**。
9. 审核可「重做混剪」（再发一条任务，不删旧草稿）。发布文案来自最近二创结果。

选题 UI 已卸（`web/src/idea/` 未挂 `App.tsx`）。不要恢复「给我选题」。后端 `/api/ideas` 还在，openai_compat 不认 `topic_card`。

### 3.2 电影混剪（已屏蔽）

界面入口已关掉，旧 `/projects?mode=movie` 会回到风景混剪。代码和 `jianying-movie-montage` 仍保留，后续再开。

按口播从 `catalog.db` 检索（标签四级池 + 全库向量近邻，最多扫 2000 镜）。无就绪镜头直接失败。改 embedding 地址/模型/密钥后必须重启控制台，否则计划里没有 `embedding_pool`。建库与混剪必须同一向量模型。

### 3.3 图文制作（ZIP）

`/image-projects`：风景混剪看板右上角「图文制作」进入。一键：粘贴定稿文案 → 文本模型起名、分段、提示词、5 条发布候选 → 并发生图（最多 18 张）。高级手动：`/image-projects/advanced`。详情 `/image-projects/{id}`。全部 ready 后打包 ZIP。详情页**没有**剪映面板。

生图走设置里的生图 Base URL / 模型（默认 `gpt-image-2`）/ API Key。分段另用「图文文本模型」；两套都空会失败。Key 只在后端加密保存，页面只显示是否已配置。

### 3.4 图文视频（已屏蔽）

界面入口已关掉，旧 `/image-videos` 会回到风景混剪。后端和草稿脚本仍保留，后续再开。不要用这条线做当前日产。

原先产品设计（未对用户开放）：粘贴完整口播，选账号，选「图片视频」或「图生视频」。

服务端流程：

1. 配音（火山 / AuraSTD，与混剪同一套 TTS 设置）。
2. 按词级 timing 切镜头：前 30 秒每张目标 4.3–4.6 秒，之后每张至少 6 秒。张数由口播时长决定，不受 18 限制，硬顶 60。
3. 文本模型为每段写生图提示词（赤墨风、9:16、中文标题印在图内）。模型失败则本地兜底提示词。
4. 并发生图。图片齐后自动开 `imagevideo` 作业。
5. **图片视频**：图片 + 轻推拉/淡入淡出，无字幕轨、无可编辑标题。
6. **图生视频**：`grok-imagine-video-1.5`、480p、24fps、6/10/15 秒档、最多 6 路并发。失败**不会**降级成图片视频。
7. 全部镜头成功才构建并登记剪映草稿（内置模板 `jianying-image-video-v1`）。登记失败可单独重试，不重做配音和生图。

当前本机日志常有 `image_video_ffmpeg_not_configured`：图片视频仍可测，图生视频要先配 FFmpeg/FFprobe 和 Grok 视频接口。测试用本仓库 exe，不要用伙伴 0.1.15。

API：

- `POST /api/image-videos` 创建并后台跑（202）
- `GET /api/image-videos` 只列出 `run_mode=video` 的项目
- `GET /api/image-projects` 不包含图文视频项目
- `POST /api/image-projects/{id}/image-video-jobs` 仍可用于已有图片的作业（图文视频工作室在图片齐后会自动调）

### 3.5 素材库

电影/B-roll/图片放进 `media_root/originals/{movies,broll,images}/`。风景混剪看板「素材库 → 开始建库」跑完整三步：扫描 → FFmpeg 切镜抽帧 → 视觉打标 + 向量。看 `ready_shots`，不要只看 `state=ready`。识别全失败会记 `analysis_all_failed`。

云机建库用独立程序 `cmd/catalog-builder`（默认 `:2031`），下载结果包（无成片、无密钥），本机同一程序「合并到本机」。控制台没有合并 UI。完整电影和原音轨不上云。未知许可只标 `local_draft_only`。

### 3.6 剪映登记

生成明文草稿和登记到剪映是两步。明文在、登记失败时用「只重试登记」，不要重跑整条混剪。控制台不自动打开剪映。「打开目录」只允许本机回环浏览器触发。

machine profile 的 `python_binary` 必须是已装 `pyJianYingDraft` 的绝对路径；保存后重启。不要依赖启动终端的 PATH。

---

## 4. 代码地图

### 4.1 进程

| 入口 | 路径 | 作用 |
|---|---|---|
| 控制台 | `cmd/console/main.go` | HTTP；子命令 `montage-script-run` / `openai-compat-run` / `pi-run` |
| 建库小站 | `cmd/catalog-builder` | `:2031`，不读 `console.db` |
| 维护 | `cmd/maintenance` | SQLite backup/check/restore |

### 4.2 后端包

| 包 | 职责 |
|---|---|
| `internal/app` | 根 mux、素材库服务 |
| `internal/httpapi` | REST：auth / projects / tasks / imageprojects / imagevideos / narration / media_catalog… |
| `internal/domain` | 纯数据与阶段规则 |
| `internal/store` | SQLite、迁移、`runImmediate` |
| `internal/auth` `internal/security` | 会话、CSRF、口令、脱敏、DPAPI |
| `internal/settings` | public/secret、Runtime 快照 |
| `internal/codex` | 任务 manifest、调度、结果信封 |
| `internal/agentruntime/openaicompat` | 二创写稿 |
| `internal/agentruntime/montageplan` | v1 `Build`、v2 `BuildV2`、召回 |
| `internal/agentruntime/montagescript` | 调 Python skill |
| `internal/mediacatalog` | `catalog.db`、建库、召回 |
| `internal/montage` | 剪映登记完成门 |
| `internal/imageproject` | 图文 ZIP 规划/生图；`videosceneprompts.go` 给图文视频写带标题提示词 |
| `internal/imagevideo` `internal/grokvideo` | 图文视频作业、图生视频客户端、草稿构建 |
| `internal/narration` | TTS、词级 timing、SRT |
| `internal/webui` | `go:embed` 前端 |

### 4.3 前端 `web/src/`

| 目录 | 职责 |
|---|---|
| `App.tsx` | 认证、主题、URL、WS、弹窗 |
| `production-modes/` | 目录仍列四条线；电影混剪、图文视频标 `paused`。`ModeHome` **未挂载** |
| `shell/` | 默认风景混剪看板；右上角切图文制作 |
| `project-workbench/` | 五阶段混剪工作台（界面只走风景） |
| `image-mode/` | 图文 ZIP；`ImageVideoStudio` **未挂载** |
| `media-library/` | 素材库面板（无独立 URL） |
| `settings/` | 设置 |
| `idea/` | **未挂载**，不要接回 |

看板五段：`script → assets → mixing → review → published`。后端遗留 `topic` 映射成 `script`。

### 4.4 任务实际走哪条运行时

| 任务 | 实际 | 备注 |
|---|---|---|
| `remix.*` | 硬切 `openai-compat-run` | 配了 grok 也不回退 Codex |
| `montage.execute` | 默认本机 `script` | `VIDEO_CONSOLE_MONTAGE_RUNTIME` |
| `montage.plan` | `Select` 说 script，`CommandFactory` 仍走 Codex | 已知不一致 |
| 图文 ZIP / 图文视频 | 不走 `codex_tasks` | 自有表和 worker |

密钥：二创/生图/视觉/向量/火山走设置页 `encrypted_secrets`（Windows DPAPI）。不要写进仓库或文档。

`schemas/` 里的 JSON Schema **运行时不校验**。Go 与前端类型手写对齐，守卫在 `internal/httpapi/contract_types_test.go`。

---

## 5. 当前进度与下一步

| 线 | 状态 |
|---|---|
| 风景混剪 | 日产能用，**当前默认入口** |
| 图文 ZIP | 日产能用，看板右上角切换 |
| 图文视频 | **界面已屏蔽**，代码保留，后续再开 |
| 电影混剪 | **界面已屏蔽**，代码保留，后续再开 |
| 建库 | 能建库（风景看板「素材库」） |
| 伙伴 EXE | 不在本 worktree |

建议顺序：风景混剪日产 → 图文 ZIP 刷新/继续生成/打包。不要测 `/image-videos` 或电影混剪按钮。

---

## 6. 不合理或遗留（先记着，未改代码）

这些是产品/结构债，不是这次整理要修的 bug。

1. **电影混剪、图文视频界面已屏蔽。** 旧 `/projects?mode=movie`、`/projects?mode=image-video`、`/image-videos` 改写到风景混剪。后端 API 未删。
2. **图文视频复用 `image_projects` 表，`run_mode=video`。** ZIP 与视频挤在一张表；列表靠过滤。`SuggestMontagePrompts` 禁止图内中文，图文视频改走 `SuggestVideoScenePrompts`，旧函数目前只有测试在用。
3. **图文视频作业 API 双挂。** `imageProjectsHandler` 和 `NewImageVideoJobsHandler` 都注册了 `/api/image-video-jobs/{id}`。
4. **`image_count` 库约束 1–60，应用 ZIP 封顶 18。** 视频线用满 60。
5. **风景线不按口播选画面**，电影线才向量召回。库里没有住宅/法拍时，买房口播对不到房子。
6. **口播字幕轨关闭**（排版未过关）。混剪不要再往画面画白字。
7. **FFmpeg 未配置时图生视频不可用**，启动只打 WARN，图片视频仍开。
8. **根目录堆着文稿、scratch、`.tmp_*`。** 不要提交。已写入 `.gitignore`。
9. **分支超前 origin 13 个提交几乎全是设计稿。** 工作区还有大量未提交实现（见下一节）。
10. **不要做：** 二创改回 Codex、恢复选题主路径、把电影混剪/图文视频入口提前接回、设置页 runtime 下拉、P2-5b Go→TS 代码生成、把图文视频登记走回风景四类输入。

---

## 7. 未提交代码统计（2026-08-20）

相对 `origin/codex/video-production-console`：本地分支 **ahead 13**（多为 `docs: plan/design …`）。工作区相对 HEAD（整理文档、写入 `.gitignore` 之后）：

| 类别 | 规模 | 内容 |
|---|---|---|
| 已跟踪改动 | 约 58 改 + 31 删 | 图文视频接入、词级 timing、混剪小改、前端四入口、**删掉旧文档** |
| 未跟踪实现 | 约 28 项 | `internal/imagevideo/`、`grokvideo/`、`imagevideos.go`、`scripts/image-video-draft/`、`ImageVideoStudio.tsx`、嵌入 JS/CSS、本说明 |
| 已忽略、不要提交 | 文稿、`scratch/`、`.tmp_*`、`tmp_*` | 根目录口播稿和调试垃圾，已进 `.gitignore` |

**可以以后提交的核心（不要带密钥/exe/文稿）：**

- 图文视频后端 + 前端工作室 + 迁移 `run_mode=video`
- 词级 timing 资产、混剪/二创相关已改文件
- 嵌入前端 dist
- 本文件 `docs/项目说明.md`

**不要提交：** `dist/*.exe`、`video-console-data/`、`catalog-builder.config.json`、文稿、scratch 样片、`.tmp_*`。

禁止擅自 `git reset / checkout / restore / stash / clean`。

---

## 8. 启动、验证、发布

```powershell
# 日常（仓库根目录）
$env:VIDEO_CONSOLE_INITIAL_PASSWORD = "123456"   # 仅库里还没有管理员时
.\dist\video-production-console.exe

# 改前端后同步 exe 内页面
npm --prefix web run build:embed
go build -o .\dist\video-production-console.exe .\cmd\console

# 基线（不覆盖嵌入 dist）
.\scripts\verify-baseline.ps1

# 聚焦
go test ./internal/imagevideo ./internal/imageproject ./internal/httpapi ./internal/store ./internal/app -count=1
go vet ./...
npm --prefix web run typecheck
npm --prefix web run test
```

自动测试用 fake，**不等于**剪映 / 火山 / 生图供应商已验收。

正式发布：`.\scripts\release.ps1 -Version x.y.z`，或 `build:embed` + `check-embedded-dist.ps1 -Strict` + `go build`。

Node 若不在 PATH：`C:\nvm4w\nodejs`。

---

## 9. 排障（高频）

1. 先 `GET http://127.0.0.1:2030/api/health` → `200 {"status":"ok"}`。受保护 API 返回 401 只是未登录。
2. 连不上 2030：看端口，确认没有第二实例锁住 SQLite，从仓库根启动已重建的 exe。
3. 刷新项目页 404：exe 里没有 SPA 回退，或只改了 Vite 没 `build:embed`。
4. `No module named 'pyJianYingDraft'`：machine profile 的 Python 绝对路径不对，保存后重启。
5. 混剪登记失败且明文已在：只重试登记。
6. 本机建库后风景仍偏风景：风景线本来就不按口播选画面。电影混剪入口当前已屏蔽。
7. 打开 `/image-videos` 却看到风景看板：这是当前屏蔽行为，不是走错线。
8. 口播稿正常、剪映字幕从词中间切开（「中国货 / 币锚点」「配置90 / %的媒体」）：旧判定把带数字的行级 SRT 当成整句，粘合后再按字切开。现已按内容字预算识别行级 SRT。重编 exe 并重启后「重做混剪」，不要只重试登记。

同一来源 10 分钟 5 次密码失败会锁 15 分钟。

---

## 10. 工作区红线

- 不要破坏性 git 命令，不要删 `video-console-data/` 与 `internal/webui/dist/`。
- 不要提交密钥、exe、文稿。
- 不要把伙伴 EXE 和本机图文视频测试混成一套程序。
- 局域网访问须在设置里显式改监听地址并重启。
- UI 设计系统：`DESIGN.md`。
